<h2>ログイン</h2>

<%= form_with model: @user, url: user_session_path do |f| %>
  <table>
    <tr>
      <th>メールアドレス</th>
      <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
    </tr>
    <tr>
      <th>パスワード</th>
      <td><%= f.password_field :password, autocomplete: "current-password" %></td>
    </tr>
  </table>
  <div class="actions">
    <%= f.submit "ログイン" %>
  </div>
<% end %>

<h5 class="m-3"><span class="bg-light">会員登録がお済みでない方</span></h4>
        <p class="ml-5"><%= link_to 'こちら', new_user_registration_path %>  から新規登録を行なってください。</p>
